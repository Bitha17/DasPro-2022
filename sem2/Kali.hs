module Kali where
-- KALI - kali(a,b)
-- DEFINISI DAN SPESIFIKASI
kali :: Int -> Int -> Int
{-  kali(a,b) menerima masukan 2 buah bilangan bulat (int) a, b 
    dan menghasilkan nilai a * b -}
-- REALISASI
kali a b = a*b
-- APLIKASI
-- kali 2 5